# ads.txt — 手順ナビ (it-howto-ads)
#
# このファイルはプレースホルダです。Google AdSense の掲載が承認されるまで、
# 有効な publisher ID は書きません（推測や仮の ca-pub / pub- 番号は禁止）。
#
# ---------------------------------------------------------------------------
# 承認後にやること（推奨: 静的テンプレートを差し替える）
# ---------------------------------------------------------------------------
# 1. AdSense 管理画面でサイトの ads.txt 行をコピーする
#    （サイト → ads.txt、または アカウント → ads.txt）
# 2. 下のコメント例を、AdSense が表示した 1 行に置き換える
# 3. このファイルをコミットしてデプロイする（または Vercel で再ビルド）
#
# AdSense が出す行の形（数字は必ず管理画面の値。ここに記入しない）:
#
#   google.com, pub-XXXXXXXXXXXXXXXX, DIRECT, f08c47fec0942fa0
#
# 注意:
# - ads.txt では pub-… を使います（タグの ca-pub-… から ca- を除いた値）。
# - 末尾の f08c47fec0942fa0 は Google 共通の認証局 ID で、全パブリッシャー同じです。
# - コメント行（# で始まる行）はクローラに無視されます。
#
# ---------------------------------------------------------------------------
# 別案: ビルド時に環境変数から生成する
# ---------------------------------------------------------------------------
# PUBLIC_ADSENSE_CLIENT（ca-pub-数字）がセットされているときだけ
# src/lib/adsense.ts の adsTxtLine() で上記 1 行を出す方法もあります。
# このリポジトリはプレビューに誤った ID を出さないよう、静的テンプレートを優先しています。
# 手順は README の「Google AdSense」を見てください。
#
# （承認前は、このファイルに google.com のレコードを追加しないでください）
